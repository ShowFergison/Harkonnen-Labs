//! Retrieval and briefing assembly boundary.
//!
//! `build_targeted_briefing` is currently implemented in the orchestrator so the
//! active run path stays stable. This module is the destination for that logic
//! once the call sites finish moving behind memory-owned APIs.
